spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb;MODE=PostgreSQL

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        nationalization-support: false
        dialect: org.hibernate.dialect.PostgreSQLDialect
