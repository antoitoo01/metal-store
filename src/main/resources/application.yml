server:
  port: 8080

spring:
  application:
    name: metal-store
  profiles:
    active: dev

  jpa:
    hibernate:
      ddl-auto: update

supabase:
  url: ${SUPABASE_URL:}
  anon-key: ${SUPABASE_ANON_KEY:}
  service-role-key: ${SUPABASE_SERVICE_ROLE_KEY:}
